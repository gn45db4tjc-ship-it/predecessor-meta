# 2.21.4 — verification status follows saved advice

After a failed local refresh, the application preserves its last working bundle and marks it as reviewed for a saved patch. The engine previously treated that status as an active current review, allowing old builds and role comparisons to continue as if verification had succeeded. Current advice now requires successful live verification, a current review status and no newer public patch/hotfix hold. Dated plans, source variants and observations remain inspectable. A successful verification restores the normal recommendations.

The same check covers builds, hero strategy, reviewed compositions, ability sequence and damage reviews, draft comparisons and item evidence. An automatic starting build requires eligible Statz observations when no authored plan is active. Retained or undated variants remain inspection choices. Inspection-only item sample counts cannot break ties between automatic purchases. The unavailable-build view offers the previous reviewed plan directly, without six empty purchase slots.

No strategy was re-authored or review date advanced. Fresh role statistics and the daily cloud schedule remain unchanged. Pred.gg authorized access and the pending choice about daily Codex reviews remain unresolved.

Verification: 29 focused JavaScript tests and 61 Python publication tests pass from a clean source ZIP. The prior fixtures now include the valid outer guidance patch, as real bundles do. A separate evaluation checked all 85 existing hero/role plans in each of six actual published bundles: current planned builds match 2.21.3 exactly; after a simulated failed verification none remains active, and source records are unchanged. Browser checks confirmed unavailable builds, expansion of historical advice and recovery to the current view, with no JavaScript errors. Full historical browser/device suites were not repeated.

This is based on 2.21.3 and preserves Design Revision 2, Builds, Live game, saved data/settings and authored guidance. The source-only archive includes the current code and regression checks; deployment receipts and real-data test artifacts remain outside the release ZIP.
