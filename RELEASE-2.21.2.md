# 2.21.2 — daily cloud collection

Available sources now collect daily in GitHub for all six brackets, even with the owner's PC off. Pred.gg alone stays paused pending authorized access; no requests are sent to its paused collector. Its previously saved observations retain their original dates and the existing strict same-patch retention rules. Statz/Omeda statistics, kits, catalogues, community builds and official source checks remain independent. No rates, kit-fit formulas or authored strategic claims were changed.

The daily target is 17:23 UTC. Official live patch and hotfix checks run every three hours; changed released content triggers another collection. Schedules can be delayed and sources can fail. On opening, the shared site reads the latest publication, and visible tabs check for changes every five minutes. Fetch time does not establish a source's statistical match window.

An old Windows receipt can no longer move the cloud's daily-attempt clock or per-bracket status backwards. Historical complete bundles remain separate from useful partial updates. Windows collection remains available as an additional route; it is no longer required for the cloud to collect supported sources.

The website describes the cloud schedule and separately identifies Pred.gg's pause. Daily calculated recommendations use published evidence. Authored plans are still dated reviews; a data refresh does not silently re-author or re-certify them. A daily Codex strategy review is a separate user choice and uses the existing Codex allowance when the host is available.

Validation: 61 Python publication tests passed, including all six cohorts, no network call from a paused source, preserved source dates, one daily attempt and monotonic Windows/cloud imports. The seven existing JavaScript independent-source tests passed. Source-only packaging repeats these checks from a clean extraction. Design Revision 2, engine.js, ui.js, ui.html and reviewed_guidance.json are preserved byte for byte from 2.21.1. Prior broader browser and regression evidence remains historical; this is not a claim to have rerun it all.

Deployment and live-cloud acceptance are recorded in the separate local release receipt; do not treat this source report alone as proof of installation. Rollback restores program files without deleting saved data, snapshots or settings.
