# 2.31.3 — restore reviewed starting builds after a source refresh

The Windows refresh rejected every starting build because stored preconditions used older descriptions. This release reconciles 106 individually inspected source-field transitions while retaining exact checks for unsupported changes. The saved review dates and all observed samples remain unchanged.

The reproduced Windows data now supports 85 of 94 starting plans. Phase Support and Valmont Mid remain inspection-only because source mechanics disagree. Seven previously unresolved experimental roles also retain dated fallback guidance. Tier rankings, full team advice and automatic match adaptations have separate review requirements; this repair does not mark them current.

The status strip separates build availability from the older tier/team review. Meta says “Tier review pending”; a held match-adaptation coach no longer suggests an otherwise available starting build is missing. Supporting source checks are available inside each affected build.

The Pred.gg parser no longer narrows “Ability Damage” to magical damage. The saved-data repair is guarded by the exact stored raw source and does not override independent official corrections.

See `docs/GUIDANCE-AVAILABILITY-REPAIR.md` for evidence, boundaries and regression coverage. Installation requires a hashed backup and preservation of data/settings; public publication remains a separate action. Rollback restores the previous program and guidance files, never old statistics.
